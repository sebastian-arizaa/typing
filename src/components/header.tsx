export function Header() {
  return (
    <header>
        <nav className="flex justify-center py-2 px-[10%] bg-primary text-white">
          <span className="text-4xl font-semibold">Typing</span>
        </nav>
    </header>
  )
}
