export type CurrentPage = 'level' | 'levels'
export type TypeLetter = {
  letter: string, 
  correct: boolean,
  inCorrect: boolean,
  hover: boolean,
}
export type LevelType = {level: string, title: string, content: string} 
